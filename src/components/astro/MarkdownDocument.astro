---
import MarkdownRenderer from "@/components/MarkdownRenderer";

interface Props {
  title: string;
  dek?: string;
  content: string;
}

const { title, dek, content } = Astro.props;
---

<main class="min-h-screen px-3 py-10 text-[color:var(--ink)] sm:px-4">
  <div class="mx-auto w-full max-w-[1040px] space-y-6">
    <header class="space-y-3 text-center sm:text-left">
      <p class="text-[0.65rem] uppercase tracking-[0.35em] text-[color:var(--accent-blue)] sm:text-xs">Official Notice · Issue XXXVII</p>
      <h1 class="wobbly-underline text-[clamp(2rem,5vw,3rem)] font-semibold leading-tight text-[color:var(--ink)]">{title}</h1>
      {dek && <p class="text-base font-serif italic text-[color:var(--ink-soft)] sm:text-lg">{dek}</p>}
    </header>

    <section class="rounded-[26px] border border-[color:var(--coffee-light)] bg-[color:var(--paper)]/90 p-5 shadow-[0_15px_35px_rgba(35,24,21,0.12)] sm:rounded-[32px] sm:p-8">
      <MarkdownRenderer content={content} />
    </section>
  </div>
</main>
