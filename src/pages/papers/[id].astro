---
import BaseLayout from "@/layouts/BaseLayout.astro";
import MigrationPlaceholder from "@/components/astro/MigrationPlaceholder.astro";

export const prerender = false;

const id = Astro.params.id ?? "unknown";
---

<BaseLayout title="Paper Detail | The Journal of AI Slop™">
  <MigrationPlaceholder
    title={`Paper ${id}`}
    message="Astro SSR/ISR paper detail rendering ships in PR 2."
  >
      <a href="/papers" class="text-[0.75rem] font-semibold uppercase tracking-[0.25em] text-[color:var(--accent-blue)] underline">Back to Papers</a>
  </MigrationPlaceholder>
</BaseLayout>
